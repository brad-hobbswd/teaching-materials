/* ==========================================
   LITTLE EXPLORERS LEARNING HUB
   About Page JavaScript
========================================== */

/*
   Global navigation, sticky header, smooth scrolling,
   and section reveal behavior are handled by the
   site's shared root script.js.

   This file is intentionally reserved for About-page
   specific behavior so the page does not register
   duplicate navigation handlers.
*/

document.addEventListener("DOMContentLoaded", () => {

    /* About-page-specific behavior can be added here. */

});
